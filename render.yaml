services:
  - type: worker
    name: discord-license-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: DISCORD_TOKEN
        sync: false
      - key: KEY_PREFIX
        value: ADAMCORP
      - key: KEY_LENGTH
        value: "5"
      - key: REQUIRED_ROLE_NAME
        value: Reseller
